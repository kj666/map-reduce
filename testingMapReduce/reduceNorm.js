function reduceNormalize(key, values){
    var norm = [];
    values.forEach(function(val){
        // if(val.type) v c

        norm.push(val.type);
        // if(val < min) min = val;
    });
    return {values};
}